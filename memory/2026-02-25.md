- Fixed tracked X source handle ingestion path end-to-end (dashboard + polymarket UI + API + pipelines): supports @handle and x.com/twitter URLs, normalizes to canonical lowercase handle key, and prevents case-variant split rows.
- Verified live save path for `x.com/NoLimitGains` and `@Qullamaggie` via API and browser-level dashboard interaction.
- Added Kaggle ingest gating for API-safe cadence:
  - `scripts/run_kaggle_ingest.sh` now enforces min-hours gate and daily download limit using `execution_controls` runtime keys.
  - Added configurable controls: `kaggle_min_hours_between_runs`, `kaggle_daily_download_limit`, `kaggle_max_files_per_run`, `kaggle_max_rows_per_file`.
  - Added `FORCE_KAGGLE_INGEST=1` manual override for backfill/testing.
- Integrated Kaggle ingest into `run-all-scans.sh` as a safe gated step.
- Added file/row limits and newest-first ingestion behavior in `pipeline_j_kaggle_polymarket.py`.
- Added operator docs:
  - `docs/KAGGLE-DATA-FIT-GUIDE.md`
  - Updated `docs/FREE-TRAINING-STACK-ROADMAP.md` with new controls and cadence behavior.
- Full pipeline audit run completed; system remains operational with `overall=warn` due trade-claim guard state (`no approved queued routes`), not ingestion breakage.
- GitHub parity check: `main` and `origin/main` are in commit sync (`0 ahead / 0 behind`), but local working tree has uncommitted changes.
- Added `scripts/pipeline_digest_check.sh` and wired it into `run-all-scans.sh` so each cycle emits a compact good/warn/bad ingestion health summary from DB freshness/throughput.
- Verified digest currently reports `overall=good` with fresh pipeline/candidate/route timestamps.
- Kaggle daily gate verified: second run now correctly skips with `reason=min_hours_gate`.
- UI reliability fix for X handle save actions: dashboard boot could fail before wiring source buttons when any Polymarket API call rejected; patched `dashboard-ui/static/polymarket.js` to use safe fetch fallbacks and wire handlers before render blocks.
- Hardened main dashboard UI bootstrap in `dashboard-ui/static/app.js` with per-step guard rails (`runUiStep`) so render errors no longer block `btn-x-save` binding.
- Eliminated stale JS cache risk by setting no-cache headers in `dashboard-ui/app.py` and bumping static script versions to `20260225c` in `index.html` and `polymarket.html`.
- Verified live with Playwright on 2026-02-24 PT: both `/` and `/polymarket` Add/Update source buttons submitted `@Qullamaggie` successfully and returned saved status.
- Added resilient handle extraction in UI (not ID-dependent): source save now scans handle-like fields in the same card if primary ids are missing; prevents false empty-handle errors when markup drifts.
- Added visible build tag in handle-required messages (`[build 20260225d]`) for quick stale-client detection during support.
- Hardened learning resolver runtime for production safety:
  - Added `learning_resolver_http_timeout_seconds`, `mtm_resolver_max_runtime_seconds`, `missed_opportunity_max_runtime_seconds`, and `horizon_resolver_max_runtime_seconds` controls.
  - Added per-loop runtime cutoffs in `update_learning_feedback.py` so daily learning jobs cannot run indefinitely.
- Improved horizon resolver price coverage:
  - Expanded equity at-time lookup to nearest-bar lookahead window (up to 3 days) to handle after-hours/weekend routed timestamps.
  - Added crypto-specific at-time/latest price lookups and wired by ticker/venue detection.
- Hardened `scripts/run_learning_feedback_daily.sh`:
  - Uses `python3.11` when available.
  - Adds PID-aware lock handling and stale lock cleanup.
- Validated daily learning run after hardening:
  - `horizon_resolved=205` in latest run.
  - `route_outcomes_horizons` now includes 6h and 24h buckets (`6h=259`, `24h=17`; later `horizon_rows=276` in digest).
- Source-of-truth posture remains intact:
  - `route_outcomes` remains operational-only in this dataset (`realized=0`, `operational>0`), so GRPO readiness gate still warns on `realized_lt_min`.
- Fixed GRPO readiness false-red condition:
  - `scripts/grpo_readiness_gate.sh` now treats Kaggle as healthy when `runtime:kaggle_last_success_utc` is recent (control: `grpo_kaggle_max_success_age_hours`, default 48h), even if latest status is `skipped:min_hours_gate`.
  - Readiness reasons now correctly isolate the true blocker: `realized_lt_min`.
